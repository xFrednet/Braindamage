[package]
name = "braindamage"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.38", features = ["derive"] }
expect-test = "1.5.1"
itertools = "0.14.0"
regex = "1.11.1"

[dev-dependencies]
assert_cmd = "2.0.17"
insta = { version = "1.43.1", features = ["glob"] }
